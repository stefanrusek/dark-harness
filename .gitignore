node_modules/
dist/
coverage/
# DH-0061 overnight-verification spike evidence (screenshots) — run output, never committed.
e2e/spikes/web/artifacts/
# DH-0061 orchestrator's comprehensive report — regenerated by every run-all.ts run.
e2e/spikes/web/REPORT.html
*.log
.DS_Store
.dh-logs/

# Gitignored secrets file for --env; dh.json's $(VAR) interpolation reads secrets from it.
secrets.env
*.env

# Local agent-harness tooling artifacts (isolated worktrees for parallel domain leads) —
# not project deliverable content, never committed.
.claude/*
!.claude/skills/
# Except project-local skills, which are committed process tooling (see tracking/DH-0008).
__pycache__/

# Local dh.json (operator's own model/provider config for manual testing) — never committed.
/dh.json
