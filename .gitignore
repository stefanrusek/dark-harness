node_modules/
dist/
coverage/
*.log
.DS_Store
.dh-logs/

# Gitignored secrets file for --env; dh.json's $(VAR) interpolation reads secrets from it.
secrets.env
*.env

# Local agent-harness tooling artifacts (isolated worktrees for parallel domain leads) —
# not project deliverable content, never committed.
.claude/*
!.claude/skills/
# Except project-local skills, which are committed process tooling (see tracking/DH-0008).
__pycache__/
